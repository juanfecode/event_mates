<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Event Mates" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/em-logo.png" type="em-logo/png">
    <link rel="icon" href="/em-logo.svg" type="em-logog/svg+xml">
    <link rel="apple-touch-icon" href="/em-logo.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" type="text/css" href="https://npmcdn.com/flatpickr/dist/themes/dark.css">
  </head>

  <body data-current-user-id="<%= current_user ? current_user.id : "" %>">
    <%= render "shared/navbar" %>
    <%= render "shared/flashes" %>

    <%= yield %>
    <%= turbo_frame_tag "remote_modal" %>
  </body>
</html>
